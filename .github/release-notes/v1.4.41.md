## What's changed

- Fixed resumed Discovery playback when the selected media requires transcoding.
- Discovery transcodes now seek through Tater Tube Server's authenticated, byte-range-capable virtual stream instead of treating an NZB virtual path as a local disk file.
- Preserved efficient resume seeking without decoding the video again from the beginning.
