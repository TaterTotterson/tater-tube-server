## What's changed

- Fixed resumed local-library playback when a title requires audio-only, video-only, or full transcoding.
- Local transcodes now seek the resolved media file directly instead of being routed through the Discovery/NZB virtual-stream endpoint.
- Preserved the authenticated byte-range stream introduced for resumed Discovery playback.
