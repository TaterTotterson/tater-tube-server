services:
  tater-tube-server:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: tater-tube-server
    volumes:
      - ./config:/config
    ports:
      - "8080:8080"
    restart: unless-stopped
